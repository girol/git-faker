# Configurations for running commands

# Git readable url to remote repository to testing code
remote_repo="git@github.com:username/repo.git"

# Local repository to mirror locally
local_repo=~/workspace/repo

# A common pipeline for code deployment
release_pipeline=('master' 'testing' 'beta' 'production')

# Maximum number of objects created in an iteration (creating commits for example)
max_objects=3